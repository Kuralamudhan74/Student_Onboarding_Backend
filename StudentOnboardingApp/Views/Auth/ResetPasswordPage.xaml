<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:StudentOnboardingApp.ViewModels.Auth"
             x:Class="StudentOnboardingApp.Views.Auth.ResetPasswordPage"
             x:DataType="vm:ResetPasswordViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource LightBackground}">

    <VerticalStackLayout Spacing="20" Padding="30" VerticalOptions="Center">

        <Label Text="Reset Password"
               FontSize="28"
               FontAttributes="Bold"
               TextColor="{StaticResource Primary}"
               HorizontalOptions="Center" />

        <Label Text="Enter your new password below."
               FontSize="14"
               TextColor="{StaticResource Gray500}"
               HorizontalOptions="Center" />

        <Label Text="{Binding ErrorMessage}"
               TextColor="{StaticResource ErrorRed}"
               IsVisible="{Binding ErrorMessage, Converter={StaticResource IsNotNullConverter}}"
               HorizontalOptions="Center"
               FontSize="13" />

        <Border Stroke="{StaticResource Gray200}" StrokeShape="RoundRectangle 12" Padding="12,4">
            <Entry Placeholder="New Password" Text="{Binding NewPassword}" IsPassword="True" ReturnType="Next" />
        </Border>

        <Border Stroke="{StaticResource Gray200}" StrokeShape="RoundRectangle 12" Padding="12,4">
            <Entry Placeholder="Confirm New Password" Text="{Binding ConfirmNewPassword}" IsPassword="True" ReturnType="Done" />
        </Border>

        <Button Text="Reset Password"
                Command="{Binding ResetPasswordCommand}"
                BackgroundColor="{StaticResource Primary}"
                TextColor="White"
                FontAttributes="Bold"
                CornerRadius="12"
                HeightRequest="50"
                IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}" />

        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="{StaticResource Primary}" HorizontalOptions="Center" />

    </VerticalStackLayout>
</ContentPage>
